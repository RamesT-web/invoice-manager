# Database — Replace with your PostgreSQL connection string
# For local Docker: postgresql://postgres:postgres@localhost:5432/invoice_manager
# For Supabase:     postgresql://postgres:[PASSWORD]@db.[REF].supabase.co:5432/postgres
# For Render:       (use the Internal Database URL from your Render dashboard)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/invoice_manager"

# NextAuth — Generate a secret: openssl rand -base64 32
NEXTAUTH_SECRET="change-this-to-a-random-secret-string"
# Set this to your deployed URL in production (e.g. https://invoice-manager.onrender.com)
NEXTAUTH_URL="http://localhost:3000"

# App URL (same as NEXTAUTH_URL)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# File Storage Driver: "local" (default) or "object" (S3-compatible)
# Use "local" for laptop-first, "object" for cloud deployment
STORAGE_DRIVER="local"

# S3-compatible Object Storage (only needed when STORAGE_DRIVER=object)
# Works with: Supabase Storage, Cloudflare R2, AWS S3, MinIO
# S3_ENDPOINT="https://xxx.r2.cloudflarestorage.com"
# S3_ACCESS_KEY_ID="xxx"
# S3_SECRET_ACCESS_KEY="xxx"
# S3_BUCKET_NAME="invoice-manager"
# S3_REGION="auto"

# (Future) Email
# RESEND_API_KEY="re_xxx"

# (Future) Push Notifications
# NEXT_PUBLIC_FCM_VAPID_KEY="xxx"
# FCM_SERVER_KEY="xxx"
